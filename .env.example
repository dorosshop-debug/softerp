# Copia este archivo como .env y completa tus valores
# cp .env.example .env

OPENROUTER_API_KEY=sk-or-v1-TU_CLAVE_AQUI

# Clave opcional para cifrar tokens de integración (si no existe, se crea storage/app.key)
# APP_KEY=

# Las APIs de facturación (Alegra / Siigo / Factus / DIAN) se configuran
# por empresa en Contabilidad → Integraciones (no en este archivo).

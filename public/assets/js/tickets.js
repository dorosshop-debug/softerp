// Seri ERP - Módulo Tickets / Soporte
'use strict';

document.addEventListener('DOMContentLoaded', function() {
    var chat = document.querySelector('.ticket-chat');
    if (chat) chat.scrollTop = chat.scrollHeight;
});
